```
# 执行评估  评估后的结果结果保存在result中  
result = evaluate(  
    dataset=dataset,  
    llm=vllm,  
    embeddings=vllm_e,  
    # metircs修改对应的评估数据  
    metrics=[  
        context_precision,  
        context_recall,  
        faithfulness,  
        answer_relevancy,  
    ], raise_exceptions=False  
)
```